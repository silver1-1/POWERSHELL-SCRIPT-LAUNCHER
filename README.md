# POWERSHELL-SCRIPT-LAUNCHER
The PowerShell Script Launcher is an automated, menu-driven interface designed to simplify running your custom PowerShell scripts. Instead of manually navigating directories and executing scripts via the command line, this tool dynamically scans your directories and lets you run them with a single menu selection. Compatible with other .PS1 scripts.
